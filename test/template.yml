peers:
  ${peerid}: localhost:"${peer_port}"
role: "${role}"
items:
  - item: "salt"
    amount: ${salt_amount}
    unlimited: ${sale_unlimited}
  - item: "boars"
    amount: ${boars_amount}
    unlimited: ${boars_unlimited}
  - item: "fish"
    amount: ${fish_amount}
    unlimited: ${fish_unlimited}

maxpeers: ${maxpeers}
maxhops: ${maxhops}
nodeid: ${nodeid}
nodeport: ${nodeport}
